spring:  
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/KubeCloud/demo-itonk-gr4/
          searchPaths: 'cfg'
server:
  port: 8080
---
spring:
  profiles: development
server:
  port: 8888